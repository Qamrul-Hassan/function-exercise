/* 6. Write a function that takes two strings and concatenates them using `concat()`.
 Also, demonstrate how to join an array 
of strings into a single string using `join()`.*/

function str (str1, str2){
    return str1.concat(str2);
}
console.log(str("Qamrul ", "Hassan"));
